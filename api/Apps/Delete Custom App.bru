meta {
  name: Delete Custom App
  type: http
  seq: 4
}

delete {
  url: {{baseUrl}}/api/custom?name=weather
  body: none
  auth: none
}

params:query {
  name: weather
}

docs {
  Deletes a custom app by name.

  System apps (clock, date) cannot be deleted.
}
